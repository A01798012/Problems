#include <bits/stdc++.h>
using namespace std;
int main(){
  cin.tie(0); ios_base::sync_with_stdio(0);
  map<int, int> idk;
  idk[5] = 5;
  idk[2] = 5;
  auto a = idk.begin();
  cout << (*a).first << endl;
  a++;
  cout << (*a).first << endl;
  a--;
  cout << (*a).first << endl;
  exit(0);

    int m;
    cin >> m;
    map<int,int>estudiante;
        int p,x;
    for (int i = 0; i < m; i++) {
        cin >> p >> x;
        if (p == 1) {

        }
        if (p == 2) {

        }
    }

    return 0;
}
